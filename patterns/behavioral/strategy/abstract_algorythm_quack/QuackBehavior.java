package patterns.behavioral.strategy.abstract_algorythm_quack;

/* АБСТРАКЦИЯ ПОВЕДЕНИЯ "КРЯКАНЬЕ"
 * - имеет только 1 метод, представляющий выполнение данного поведения */

public interface QuackBehavior {
    void quack();
}
